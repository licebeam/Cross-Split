import React from "react";

const Splits = () => {
  return (
    <div>
      <button>Add Split</button>
      <div>
        split here <button>delete</button>
      </div>
      <button>Add Split</button>
    </div>
  );
};

export default Splits;
